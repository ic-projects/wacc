# spooky strings

# Output:
#

# Program:

begin
	pair(pair, pair) f(pair(pair, pair) x) is
		return x
	end
	pair(int, pair) f2(pair(int, pair) x) is
		return x
	end
	pair(pair, pair) n = null ;
	pair(pair, pair) n2 = fst n ;
	pair(pair, pair) n3 = call f(n2) ;
	pair(pair, pair) n4 = newpair(n, n2) ;
	pair(pair, pair) n5 = newpair(n4, null) ;
	pair(pair, pair) n6 = newpair(null, null) ;
	pair(pair, pair) n7 = call f(null) ;
	pair(int, pair) s = newpair(1, null) ;
	pair(int, pair) s2 = newpair(1, s) ;
	pair(int, pair) s3 = snd s2 ;
	pair(int, pair) s4 = call f2(s) ;
	pair(int, pair) s5 = fst n
end
